{
  "name": "backgammon",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:client dev:server",
    "dev:client": "npm --workspace client run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace shared run build && npm --workspace client run build && npm --workspace server run build",
    "test": "npm --workspace shared run test && npm --workspace server run test",
    "format": "prettier --write \"client/src/**/*.{ts,tsx,css}\" \"server/src/**/*.ts\""
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.8.1"
  }
}
